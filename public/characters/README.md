# Legacy: individual character PNGs

The home screen now uses **`/characters-group.png`** at the project root (`public/characters-group.png`) — one Figma group export for correct layout.

These single-character files are kept for reference only.
